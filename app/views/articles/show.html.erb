<p id="notice"><%= notice %></p>

<div class="card-body text-center">
    <h5 class="card-title"><%= @article.title %></h5>
    <p class="card-text"><%= @article.description %></p>
    <a href="#" class="btn btn-primary"><%= link_to 'Show', article_path %></a>
    <a href="#" class="btn btn-primary"><%= link_to 'Destroy', article_path, method: :delete, data: { confirm: 'Are you sure?' } %></a>
    <a href="#" class="btn btn-primary"><%= link_to 'edit', edit_article_path(@article) %></a>
  </div>
  <div class="card-footer text-muted text-center">
    2 days ago
  </div>

<%#%= link_to 'Edit', edit_article_path(@article) %> 
<%#<%= link_to 'Back', articles_path %> 
